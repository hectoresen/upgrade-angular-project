import { PipePricePipe } from './pipe-price.pipe';

describe('PipePricePipe', () => {
  it('create an instance', () => {
    const pipe = new PipePricePipe();
    expect(pipe).toBeTruthy();
  });
});
